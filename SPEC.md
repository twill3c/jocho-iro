# SPEC.md — jocho-iro(情緒の色)

<!-- scaffold template v1.8.0 から展開(2026-08-17)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

kokoro-graph(極性 1 次元)の姉妹アプリ。青空文庫 300 作品を **8 情緒**
(喜 joy・怒 anger・哀 sadness・怖 fear・好 like・驚 surprise・厭 dislike・安 calm)で
行単位分析し、「色相バーコードの壁」「情緒プロファイル(レーダー)」「情緒の川
(ストリームグラフ)」で可視化する静的アプリ。300 作品は情緒分析に最適な作品を
**データ駆動で再選出**する(選定自体が機械ゲート)。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | コーパス再選出: 評価母集団(kokoro-graph の 300 + 拡張候補)全てを同一物差し(ヒット行率・8 情緒プロファイル・エントロピー)で採点し、選定規則(F-02)で 300 を機械選出。全判断を `data/corpus_manifest.json` に記録 | must |
| F-02 | 選定規則: ① 計測可能性 = **総有効ヒット ≥ 30 かつ ヒット行率 ≥ 0.08**(較正 loop_001 で確定: 総ヒット N はプロファイル安定性 — N=30 で支配情緒シェアの標準誤差 ≈0.09。行率だけでは低密度大作と少ヒット短編を区別できない)② 正規化後 ≤200,000 字 ③ 各情緒の支配作を最低 8 作確保(不足は較正レポートに実数記録)④ 残枠は情緒エントロピー降順 ⑤ 同一作者上限 26 作。この順に決定論で適用 | must |
| F-03 | 情緒プロファイル: 作品の情緒 e のシェア s_e = ヒット数_e / 総ヒット数。**リフト r_e = s_e / b_e**(b_e = コーパス全体の e シェア)で語彙数の不均等を補正。支配情緒 = リフト最大の情緒。エントロピー H = −Σ s_e log2 s_e(0 log 0 = 0) | must |
| F-04 | 情緒の川: 本文を 64 区間に等分し、区間ごとの 8 情緒ヒット数を隣接区間と平滑化(中心窓・半径 2)した強度列。gold に格納 | must |
| F-05 | 辞書: kokoro-graph の kokoro_dict v1.1.0(419 語・8 カテゴリ)を共通資産として同梱。スコアリング規則(否定窓 2 形態素の反転)も同一。否定反転したヒットは**極性反転でカテゴリ不変**とせず、情緒集計から除外する(「嬉しくない」は喜の証拠にならない) | must |
| F-06 | gold 生成: `web/public/data/works/{id}.json`(行・ヒット語・カテゴリ)+ `index.json`(8 情緒リフト・エントロピー・支配情緒・ヒット行率・64 区間の川データ・図書カード URL・底本) | must |
| F-07 | UI 一覧: 色相バーコードの壁(区間の支配情緒の色相 × ヒット密度の濃さ)+ 支配情緒フィルタ(8 色チップ)+ 並べ替え(各情緒のリフト順・エントロピー順・作者順)。8 情緒の凡例を常設 | must |
| F-08 | UI 詳細: 情緒レーダー(8 軸・リフト表示・コーパス平均 = 1 の基準線)+ 情緒の川(積層ストリーム)+ 本文リーダー(ヒット語を情緒色でハイライト・行クリック ⇔ 川クリックのシンクロ)+ 底本表示 | must |
| F-09 | フッタ: MIT License・GitHub・操作説明・設計図・App Menu の 5 リンク | must |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 静的配信のみ(Next.js 15 `output: "export"` + trailingSlash)。実行時分析なし | build + 本番確認 |
| N-02 | 青空文庫アクセスは手動コマンドのみ・間隔 ≥0.8s。既取得 bronze(kokoro-graph 由来 317 件)は再取得しない | AGENTS 規約 |
| N-03 | 8 情緒の色は固定パレット(色覚多様性に配慮し明度差も持たせる)。凡例なしでも濃淡で構造が読めること | UI 確認 |
| N-04 | 純粋コア(src/core)は DOM 非依存・クライアント API は useEffect 内のみ(HC-002)。非有限値・ヒット 0 作品は正常系 | eslint + テスト |
| N-05 | 底本表記・図書カードリンクを作品詳細に表示 | UI 確認 |

## 4. 品質基準

| ID | ゲート | 基準 |
|---|---|---|
| G-01 | 選定の決定論 | 同一入力での再選出が完全一致。選定 300 が F-02 の全条件を満たす(pytest 検査) |
| G-02 | 情緒数理 | シェア・リフト・エントロピー・川の窓集計が手計算フィクスチャと 1e-9 で一致 |
| G-03 | 否定除外 | 「嬉しくない」が喜に計上されないことをフィクスチャで固定 |
| G-04 | gold 整合 | works = 300・index と works の行数/ヒット数が全作品一致・リフトの再計算一致 |
| G-05 | 較正レポート | 支配情緒の分布・各情緒の代表作上位・既知作品の妥当性(山月記 = 哀/厭系、走れメロス = 喜/怖系など)を docs に記録し人間が確認できる形にする |
| G-06 | UI | vitest(core ≥90/85)+ verify.mjs 4 ゲート + Playwright 実駆動 |

## 5. スコープ外

- 辞書の大規模増補(v1.1.0 を用いる。カテゴリ再配分が必要になれば感度分析付き改版)
- 反語・皮肉・文脈理解 / 実行時分析 / 検索・保存機能
- kokoro-graph との相互リンク以上の統合(別アプリとして独立)
